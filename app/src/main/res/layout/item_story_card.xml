<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="151dp"
    android:layout_height="wrap_content"
    android:minHeight="244dp"
    android:layout_marginEnd="12dp"
    app:cardBackgroundColor="@color/home_tag_bg"
    app:cardCornerRadius="20dp"
    app:cardElevation="0dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <View
            android:layout_width="match_parent"
            android:layout_height="190dp"
            android:background="@drawable/bg_story_thumb_rounded" />

        <TextView
            android:id="@+id/textStoryTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:maxLines="2"
            android:textColor="@color/login_text_primary"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Mặt Trăng Đỏ" />

        <TextView
            android:id="@+id/textStoryChapter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:background="@drawable/bg_home_genre_tag"
            android:paddingHorizontal="10dp"
            android:paddingVertical="3dp"
            android:textColor="@color/login_text_primary"
            android:textSize="12sp"
            tools:text="Chương 124" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
