spring.datasource.url=jdbc:mysql://localhost:3306/story_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=danghai123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT settings (use a 256+ bit secret)
app.jwt.secret=ReplaceThisWithASecureRandomLongSecretKeyAtLeast32Chars
app.jwt.expiration-ms=86400000

# Initialize schema from schema.sql
spring.sql.init.mode=always
spring.sql.init.encoding=UTF-8
spring.sql.init.schema-locations=classpath:schema.sql
